services:
  backend:
    build:
      context: ./http
      dockerfile: dockerfile.dev
    container_name: tg-parse-backend
    restart: unless-stopped
    ports:
      - "4000:4000"
    volumes:
      - ./parser/media:/app/media
    environment:
      - NODE_ENV=development
      - MONGODB_URI=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@mongo:27017
      - MONGODB_DB=${MONGODB_DB}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - JWT_ACCESS_TTL=${JWT_ACCESS_TTL}
      - JWT_REFRESH_TTL=${JWT_REFRESH_TTL}
      - YANDEX_API_FOLDER=${YANDEX_API_FOLDER}
      - YANDEX_API_KEY=${YANDEX_API_KEY}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - VITE_API_URL=${VITE_API_URL}
      - PORT=${PORT}
    networks:
      - bots_bots
  parser:
    build: ./parser
    container_name: tg-parse-parser
    restart: unless-stopped
    ports:
      - "127.0.0.1:3003:3003"
    volumes:
      - ./parser:/app
      - /app/node_modules
      - ./parser/media:/app/media
    environment:
      - NODE_ENV=development
      - STRING_SESSION=${STRING_SESSION}
      - MONGO_URI=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@mongo:27017
      - API_ID=${API_ID}
      - API_HASH=${API_HASH}
      - MEDIA_PATH=${MEDIA_PATH}
      - MONGO_DB_NAME=${MONGODB_DB}
      - API_BASE_URL=${API_BASE_URL}
      - API_USERNAME=${API_USERNAME}
      - API_PASSWORD=${API_PASSWORD}
      - STATS_UPDATE_LIMIT=${STATS_UPDATE_LIMIT}
    depends_on:
      - backend
    networks:
      - bots_bots
  frontend:
    build:
      context: ./web
      target: dev
      dockerfile: dockerfile
    container_name: tg-parse-frontend
    restart: unless-stopped
    volumes:
      - ./web:/app/
      - /app/node_modules
    ports:
      - '127.0.0.1:5174:5174'
    environment:
      - NODE_ENV=development
      - VITE_API_URL=${VITE_API_URL}
    depends_on:
      - backend
    networks:
      - bots_bots

networks:
  bots_bots:
    external: true